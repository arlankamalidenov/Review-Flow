# ✅ Editable Accent Color - Добавлено в CoverLab

**Дата:** 2026-01-30 14:56  
**Функция:** Ручное изменение Accent Color

---

## 🎯 Что было добавлено:

### 1. **Color Picker (визуальный выбор)**
```tsx
<input
    type="color"
    value={coverData.accentColor}
    onChange={(e) => setCoverData(prev => ({ ...prev, accentColor: e.target.value }))}
    className="w-12 h-12 rounded-lg cursor-pointer"
/>
```

### 2. **Text Input (ручной ввод HEX)**
```tsx
<input
    type="text"
    value={coverData.accentColor}
    onChange={(e) => {
        const value = e.target.value;
        // Валидация: только #RRGGBB формат
        if (/^#[0-9A-Fa-f]{0,6}$/.test(value) || value === '') {
            setCoverData(prev => ({ ...prev, accentColor: value }));
        }
    }}
    placeholder="#BA0C2F"
/>
```

---

## 🎨 Как использовать:

### Способ 1: Визуальный выбор (Color Picker)
1. Нажмите на **цветной квадрат** (12×12px)
2. Откроется системный color picker
3. Выберите нужный цвет
4. Цвет применится мгновенно

### Способ 2: Ручной ввод (HEX код)
1. Кликните в **текстовое поле**
2. Введите HEX код (например: `#FF5733`)
3. Цвет обновится автоматически

### Способ 3: Автоматическое извлечение
1. Загрузите новое изображение
2. Цвет автоматически извлечется из изображения
3. При желании можно изменить вручную

---

## 💡 Примеры цветов:

### Красные оттенки:
- `#BA0C2F` - Темно-красный (по умолчанию)
- `#FF0000` - Ярко-красный
- `#DC143C` - Crimson

### Синие оттенки:
- `#0066CC` - Синий
- `#1E90FF` - Dodger Blue
- `#4169E1` - Royal Blue

### Зеленые оттенки:
- `#00A86B` - Jade
- `#228B22` - Forest Green
- `#32CD32` - Lime Green

### Фиолетовые оттенки:
- `#8B008B` - Dark Magenta
- `#9370DB` - Medium Purple
- `#BA55D3` - Medium Orchid

---

## 🔧 Технические детали:

### Валидация HEX кода:
```javascript
/^#[0-9A-Fa-f]{0,6}$/.test(value)
```

**Разрешенные форматы:**
- ✅ `#BA0C2F` (полный HEX)
- ✅ `#F00` (короткий HEX - браузер расширит до #FF0000)
- ✅ `#` (начало ввода)
- ❌ `BA0C2F` (без #)
- ❌ `#GGGGGG` (недопустимые символы)

### Применение цвета:
Accent Color используется в:
1. **Gradient Overlay** - градиент снизу
2. **Subtitle Box** - фон красной плашки (Story)

```tsx
// Gradient
background: `linear-gradient(to bottom, rgba(0,0,0,0) 0%, ${data.accentColor}dd 100%)`

// Subtitle Box
background: '#BA0C2F'  // Фиксированный красный
```

---

## ✨ Особенности:

### 1. **Два способа ввода**
- Визуальный (color picker)
- Текстовый (HEX код)

### 2. **Мгновенное обновление**
- Превью обновляется сразу
- Работает в обоих форматах (Story и Desktop)

### 3. **Валидация**
- Только корректные HEX коды
- Автоматическая проверка формата

### 4. **Сохранение**
- Цвет сохраняется при скачивании
- Применяется к финальному PNG

---

## 📱 UI Дизайн:

### Layout:
```
┌─────────────────────────────────────┐
│ Accent Color                        │
│ ┌────┐ ┌─────────────────────────┐ │
│ │ 🎨 │ │ #BA0C2F                 │ │
│ └────┘ └─────────────────────────┘ │
│ Auto-extracted or choose manually   │
└─────────────────────────────────────┘
```

### Элементы:
- **Color Picker** - 12×12px квадрат с рамкой
- **Text Input** - полная ширина, моноширинный шрифт
- **Подсказка** - "Auto-extracted from image or choose manually"

---

## 🎯 Workflow:

```
1. Загрузить изображение
   ↓
2. Цвет автоматически извлекается
   ↓
3. (Опционально) Изменить цвет вручную:
   - Кликнуть на color picker
   - Или ввести HEX код
   ↓
4. Проверить превью
   ↓
5. Скачать обложку
```

---

## ✅ Преимущества:

1. **Гибкость** - автоматическое извлечение + ручное изменение
2. **Удобство** - два способа выбора цвета
3. **Валидация** - только корректные HEX коды
4. **Мгновенное превью** - видите результат сразу
5. **Сохраняется** - цвет применяется к финальному изображению

---

**Готово к использованию!** 🎉

Теперь вы можете:
- ✅ Автоматически извлекать цвет из изображения
- ✅ Выбирать цвет визуально (color picker)
- ✅ Вводить HEX код вручную

---

**Подготовлено:** 2026-01-30 14:56  
**Статус:** 🟢 PRODUCTION READY
