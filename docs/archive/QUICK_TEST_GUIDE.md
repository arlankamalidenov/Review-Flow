# 🚀 Быстрый тест Video Reels Cutter

## Шаг 1: Откройте приложение
```
http://localhost:3000
```

## Шаг 2: Откройте консоль браузера
- **Chrome/Edge**: F12 или Cmd+Option+I (Mac)
- **Safari**: Cmd+Option+C
- **Firefox**: F12 или Cmd+Shift+K (Mac)

## Шаг 3: Перейдите в Video Reels
Кликните на "Video Reels" в боковом меню

## Шаг 4: Проверьте логи
В консоли должны появиться:
```
🎬 [FFmpeg Init] Starting initialization...
✅ [FFmpeg Init] SharedArrayBuffer is available
📦 [FFmpeg Init] FFmpeg instance created
📥 [FFmpeg Init] Loading core from: https://unpkg.com/@ffmpeg/core@0.12.6/dist/esm
✅ [FFmpeg Init] Core JS loaded
✅ [FFmpeg Init] WASM loaded
⚙️ [FFmpeg Init] Calling ffmpeg.load()...
✅ [FFmpeg Init] ffmpeg.load() completed successfully
🎉 [FFmpeg Init] Initialization complete!
```

## Шаг 5: Загрузите тестовое видео
- Нажмите "Choose File"
- Выберите короткое видео (<100MB для теста)
- Дождитесь загрузки

## Шаг 6: Настройте параметры (опционально)
- Выберите фрагмент 60 секунд на таймлайне
- Если нужны субтитры:
  - Откройте Settings (⚙️)
  - Введите OpenAI API ключ
  - Включите "Enable AI Subtitles"
  - Нажмите "Generate AI Subtitles"

## Шаг 7: Запустите рендеринг
- Нажмите "Render Reel"
- Следите за прогрессом в консоли:
  ```
  [FFmpeg Progress] 0%
  [FFmpeg Progress] 25%
  [FFmpeg Progress] 50%
  [FFmpeg Progress] 75%
  [FFmpeg Progress] 100%
  ```

## Шаг 8: Скачайте результат
- После завершения появится кнопка "Download Reel"
- Нажмите для скачивания готового видео

---

## ❌ Если что-то пошло не так

### Проблема: SharedArrayBuffer недоступен
**Решение**: Перезапустите сервер
```bash
# Ctrl+C для остановки
npm run dev
```

### Проблема: FFmpeg не загружается
**Решение**: Проверьте интернет-соединение (FFmpeg загружается с unpkg.com)

### Проблема: Процесс зависает
**Решение**: 
1. Проверьте размер файла (<500MB)
2. Закройте другие вкладки браузера
3. Обновите страницу (F5)

### Проблема: Ошибка 400 Bad Request
**Решение**: Уже исправлено в коде, просто обновите страницу

---

## ✅ Ожидаемый результат

- Видео обрезано до 60 секунд
- Формат изменен на 9:16 (вертикальный)
- Субтитры добавлены (если включены)
- Файл готов к публикации в Instagram Reels/TikTok/YouTube Shorts

---

## 📊 Диагностика (если нужно)

Скопируйте и вставьте в консоль:
```javascript
console.log('🔍 FFmpeg Diagnostic');
console.log('SharedArrayBuffer:', typeof SharedArrayBuffer !== 'undefined' ? '✅' : '❌');

fetch(window.location.href, { method: 'HEAD' })
  .then(r => {
    console.log('COOP:', r.headers.get('Cross-Origin-Opener-Policy') || '❌');
    console.log('COEP:', r.headers.get('Cross-Origin-Embedder-Policy') || '❌');
  });
```

Ожидаемый вывод:
```
🔍 FFmpeg Diagnostic
SharedArrayBuffer: ✅
COOP: same-origin
COEP: require-corp
```

---

**Готово!** Если все шаги прошли успешно, Video Reels Cutter полностью функционален.
