# ✅ Fine-tune Image Position - Добавлено в CoverLab

**Дата:** 2026-01-30 14:30  
**Функция:** Точное позиционирование изображения с помощью слайдеров X и Y

---

## 🎯 Что было добавлено:

### 1. **Новые поля в интерфейсе**
```typescript
interface CoverData {
    // ... existing fields
    imagePosition: string;
    imagePositionX: number; // ✅ НОВОЕ (0-100%)
    imagePositionY: number; // ✅ НОВОЕ (0-100%)
}
```

### 2. **Значения по умолчанию**
```typescript
imagePosition: 'center center',
imagePositionX: 50,  // 50% по горизонтали
imagePositionY: 50   // 50% по вертикали
```

### 3. **Два слайдера для точного контроля**

**Horizontal (X):** 0-100%
- 0% = левый край
- 50% = центр
- 100% = правый край

**Vertical (Y):** 0-100%
- 0% = верхний край
- 50% = центр
- 100% = нижний край

### 4. **Вспомогательная функция**
```typescript
const getImagePosition = (data: CoverData): string => {
    // Если используются слайдеры (custom)
    if (data.imagePosition === 'custom') {
        return `${data.imagePositionX}% ${data.imagePositionY}%`;
    }
    // Иначе используем предустановленную позицию
    return data.imagePosition;
};
```

---

## 🎨 Как использовать:

### Вариант 1: Быстрый выбор (Сетка 3×3)
1. Нажмите на одну из 9 кнопок
2. Слайдеры автоматически установятся в соответствующие значения:
   - **Left Top** → X: 0%, Y: 0%
   - **Center Center** → X: 50%, Y: 50%
   - **Right Bottom** → X: 100%, Y: 100%
   - и т.д.

### Вариант 2: Точная настройка (Слайдеры)
1. Используйте слайдер **Horizontal (X)** для горизонтального смещения
2. Используйте слайдер **Vertical (Y)** для вертикального смещения
3. Или введите точное значение в числовое поле (0-100)
4. При изменении слайдеров `imagePosition` автоматически становится `'custom'`

---

## 💡 Примеры использования:

### Лицо человека слева вверху:
- **X: 25%** (четверть слева)
- **Y: 20%** (чуть выше центра)

### Объект справа внизу:
- **X: 75%** (три четверти справа)
- **Y: 80%** (ближе к низу)

### Горизонт на треть снизу:
- **X: 50%** (по центру)
- **Y: 66%** (две трети сверху)

### Портрет по центру, но чуть выше:
- **X: 50%** (по центру)
- **Y: 40%** (чуть выше центра)

---

## 🔧 Технические детали:

### Синхронизация кнопок и слайдеров:
```typescript
// При клике на кнопку:
onClick={() => setCoverData(prev => ({
    ...prev,
    imagePosition: 'center center',
    imagePositionX: 50,  // Синхронизируется
    imagePositionY: 50   // Синхронизируется
}))}

// При изменении слайдера:
onChange={(e) => setCoverData(prev => ({
    ...prev,
    imagePositionX: parseInt(e.target.value),
    imagePosition: 'custom'  // Переключается в custom режим
}))}
```

### CSS применение:
```tsx
<img
    src={data.image}
    style={{ objectPosition: getImagePosition(data) }}
/>

// Результат:
// Preset: objectPosition: "center center"
// Custom:  objectPosition: "50% 50%"
```

---

## ✨ Преимущества:

1. **Быстрый выбор** - 9 предустановленных позиций
2. **Точная настройка** - слайдеры с шагом 1%
3. **Числовой ввод** - можно ввести точное значение
4. **Мгновенное превью** - видите результат сразу
5. **Синхронизация** - кнопки и слайдеры работают вместе
6. **Сохраняется в экспорте** - позиция применяется к PNG

---

## 📱 UI Дизайн:

### Сетка кнопок (3×3):
- **Активная кнопка** - красная (#BA0C2F)
- **Неактивные кнопки** - белые с серой рамкой

### Слайдеры:
- **Заголовок** - "Fine-tune Position:"
- **Два слайдера** - Horizontal (X) и Vertical (Y)
- **Числовые поля** - редактируемые, 0-100%
- **Цвет акцента** - красный (#BA0C2F)

---

## 🎯 Workflow:

```
1. Загрузить изображение
   ↓
2. Выбрать примерную позицию из сетки 3×3
   ↓
3. Точно настроить с помощью слайдеров
   ↓
4. Проверить превью в реальном времени
   ↓
5. Скачать готовую обложку
```

---

## 📊 Сравнение режимов:

### Preset Mode (предустановленные позиции):
```typescript
imagePosition: 'center center'
imagePositionX: 50
imagePositionY: 50
// → objectPosition: "center center"
```

### Custom Mode (точная настройка):
```typescript
imagePosition: 'custom'
imagePositionX: 35
imagePositionY: 65
// → objectPosition: "35% 65%"
```

---

## ✅ Готово к использованию!

Теперь у вас есть **два способа** позиционирования изображения:

1. **Быстро** - выберите из 9 предустановленных позиций
2. **Точно** - настройте с помощью слайдеров X и Y

Оба способа работают вместе и синхронизируются! 🎉

---

**Подготовлено:** 2026-01-30 14:30  
**Статус:** 🟢 PRODUCTION READY
