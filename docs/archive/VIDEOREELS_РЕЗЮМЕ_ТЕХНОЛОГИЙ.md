# 🎯 VideoReels - Итоговое Резюме Технологий

**Краткое описание технологий на русском языке**

---

## 📊 Технологии по Этапам

### 🎬 ЭТАП 1: Загрузка Видео (0-10%)

#### Что происходит:
Пользователь загружает видео файл, система извлекает метаданные и создает preview.

#### Технологии:

1. **HTML5 File API**
   - **Роль:** Получение файла от пользователя
   - **Как работает:** `<input type="file">` → событие `onChange` → `e.target.files[0]`
   - **Результат:** JavaScript объект `File`

2. **FFmpeg.wasm**
   - **Роль:** Извлечение метаданных видео
   - **Как работает:** Создает `<video>` элемент, загружает файл, читает `duration`, `videoWidth`, `videoHeight`
   - **Технология:** WebAssembly (FFmpeg скомпилирован в WASM для работы в браузере)
   - **Результат:** `{ duration: 60, width: 1920, height: 1080 }`

3. **Blob URL API**
   - **Роль:** Создание временного URL для предпросмотра
   - **Как работает:** `URL.createObjectURL(file)` → `blob://localhost/xxx-xxx-xxx`
   - **Результат:** Строка URL для использования в `<video src="">`

---

### 🎤 ЭТАП 2: Генерация Субтитров (10-100%)

#### Что происходит:
Из видео извлекается аудио, отправляется в OpenAI Whisper, получаются субтитры с временными метками.

#### Подэтап 2.1: Извлечение Аудио (10-40%)

**Технология: FFmpeg.wasm**

- **Роль:** Конвертация видео в аудио
- **Как работает:**
  1. Загружает MP4 в виртуальную файловую систему FFmpeg
  2. Выполняет команду: `ffmpeg -i input.mp4 -vn -ar 16000 -ac 1 output.mp3`
  3. Читает результат из виртуальной FS
- **Параметры команды:**
  - `-vn` - убрать видео дорожку
  - `-ar 16000` - частота дискретизации 16kHz (оптимально для Whisper)
  - `-ac 1` - моно аудио (один канал)
  - `-b:a 64k` - битрейт 64 kbps
- **Результат:** MP3 blob (16kHz, mono, 64kbps)

#### Подэтап 2.2: AI Транскрипция (40-90%)

**Технология: OpenAI Whisper API**

- **Роль:** Преобразование речи в текст с временными метками
- **Как работает:**
  1. Отправляет MP3 на сервер OpenAI
  2. Модель `whisper-1` обрабатывает аудио
  3. Возвращает JSON с массивом слов и их временными метками
- **Формат ответа:**
  ```json
  {
    "words": [
      { "word": "Привет", "start": 0.0, "end": 0.5 },
      { "word": "мир", "start": 0.6, "end": 1.2 }
    ]
  }
  ```
- **Результат:** Массив слов с точными временными метками (точность до 0.01 секунды)

#### Подэтап 2.3: Оптимизация (90-100%)

**Технология: JavaScript (Алгоритм группировки)**

- **Роль:** Группировка слов в читаемые сегменты
- **Как работает:**
  1. Берет массив слов от Whisper
  2. Группирует их в сегменты по 3-5 секунд
  3. Следит, чтобы сегменты не были слишком длинными
- **Алгоритм:**
  - Если текущий сегмент + новое слово ≤ 5 секунд → добавить к текущему
  - Иначе → сохранить текущий, начать новый
- **Результат:** Массив сегментов `[{ start: 0, end: 4.5, text: "Привет мир это" }, ...]`

---

### 👁️ ЭТАП 3: Предпросмотр (Мгновенно)

#### Что происходит:
Видео и субтитры отображаются в интерактивном плеере с эффектом караоке.

#### Технологии:

1. **Remotion Player**
   - **Роль:** Рендеринг видео композиции в реальном времени
   - **Как работает:**
     - Создает композицию из нескольких слоев
     - Управляет воспроизведением покадрово (frame-by-frame)
     - Синхронизирует видео и субтитры
   - **Особенности:**
     - Работает на React
     - 30 FPS (кадров в секунду)
     - Разрешение 1080×1920 (9:16)

2. **OffthreadVideo** (компонент Remotion)
   - **Роль:** Оптимизированное воспроизведение видео
   - **Как работает:** Загружает видео в отдельном потоке (не блокирует UI)
   - **Результат:** Плавное воспроизведение без лагов

3. **SubtitleLayer** (React компонент)
   - **Роль:** Отображение субтитров с анимацией
   - **Как работает:**
     - Использует `useCurrentFrame()` для получения текущего кадра
     - Вычисляет текущее время: `frame / fps`
     - Находит активный субтитр: `subtitles.find(s => time >= s.start && time <= s.end)`
     - Рендерит текст с анимацией

4. **Karaoke Effect** (CSS + JavaScript)
   - **Роль:** Подсветка текущего слова
   - **Как работает:**
     - Разбивает текст на слова: `text.split(' ')`
     - Вычисляет индекс активного слова: `Math.floor(progress * words.length)`
     - Применяет разные стили к активному слову:
       - Цвет: `#DFFF00` (желтый) vs `#FFFFFF` (белый)
       - Шрифт: `Eurostile Italic` vs `Montserrat Bold`
       - Масштаб: `1.05` vs `1.0`

5. **Remotion Hooks**
   - `useCurrentFrame()` - текущий кадр (0-1800 для 60 секунд)
   - `useVideoConfig()` - конфигурация (FPS, разрешение)
   - `interpolate()` - плавная анимация значений (например, opacity от 0.9 до 1.0)

---

### 📦 ЭТАП 4: Экспорт в MP4 (0-100%)

#### Что происходит:
Плеер захватывается покадрово, записывается в WebM, конвертируется в MP4, скачивается.

#### Подэтап 4.1: Захват Кадров (0-70%)

**Технология: html2canvas**

- **Роль:** Преобразование DOM элемента в изображение
- **Как работает:**
  1. Находит контейнер Remotion Player в DOM
  2. Для каждого кадра (1800 кадров для 60 секунд × 30 FPS):
     - Захватывает весь контейнер как изображение
     - Рисует на canvas (1080×1920)
  3. Повторяет 30 раз в секунду
- **Особенности:**
  - Захватывает ВСЁ: видео + субтитры + анимации
  - Работает медленно (каждый кадр = новый снимок DOM)
- **Результат:** Поток изображений на canvas

**Технология: Canvas API**

- **Роль:** Рисование кадров
- **Как работает:**
  1. Создает canvas элемент (1080×1920)
  2. Получает контекст: `canvas.getContext('2d')`
  3. Рисует каждый снимок: `ctx.drawImage(snapshot, 0, 0)`
- **Результат:** Canvas с видео потоком

#### Подэтап 4.2: Запись Видео (0-70%, параллельно)

**Технология: MediaRecorder API**

- **Роль:** Запись canvas stream в видео файл
- **Как работает:**
  1. Создает поток из canvas: `canvas.captureStream(30)` (30 FPS)
  2. Создает recorder: `new MediaRecorder(stream, { mimeType: 'video/webm;codecs=vp9' })`
  3. Записывает чанки по 100ms
  4. Собирает все чанки в массив
- **Кодек:** VP9 (современный, эффективный)
- **Битрейт:** 8 Mbps (высокое качество)
- **Результат:** WebM blob

#### Подэтап 4.3: Конвертация в MP4 (70-95%)

**Технология: FFmpeg.wasm (Remux)**

- **Роль:** Преобразование WebM → MP4
- **Как работает:**
  1. Загружает WebM в виртуальную FS
  2. Выполняет команду: `ffmpeg -i input.webm -c copy output.mp4`
  3. Читает результат
- **Особенность команды `-c copy`:**
  - **НЕ перекодирует** видео (очень быстро!)
  - Просто копирует видео поток в новый контейнер
  - WebM (VP9) → MP4 (VP9)
- **Результат:** MP4 blob

#### Подэтап 4.4: Скачивание (95-100%)

**Технология: HTML5 Download API**

- **Роль:** Сохранение файла на диск пользователя
- **Как работает:**
  1. Создает Blob URL: `URL.createObjectURL(mp4Blob)`
  2. Создает `<a>` элемент
  3. Устанавливает `href` и `download` атрибуты
  4. Программно кликает: `a.click()`
- **Результат:** Файл `reel.mp4` скачан на диск

---

## 🔄 Полный Цикл Данных

```
User File (MP4)
    ↓
[HTML5 File API] → File Object
    ↓
[FFmpeg.wasm] → Metadata (duration, width, height)
    ↓
[FFmpeg.wasm] → MP3 Audio (16kHz mono)
    ↓
[OpenAI Whisper API] → Word Timestamps JSON
    ↓
[JavaScript] → Subtitle Segments (3-5s each)
    ↓
[Remotion Player] → Interactive Preview
    ├─ [OffthreadVideo] → Video Layer (z-index: 1)
    └─ [SubtitleLayer] → Subtitle Layer (z-index: 10, karaoke)
    ↓
[html2canvas] → Frame Snapshots (30 FPS × 60s = 1800 frames)
    ↓
[Canvas API] → Canvas Stream (1080×1920)
    ↓
[MediaRecorder] → WebM Video (VP9 codec, 8 Mbps)
    ↓
[FFmpeg.wasm] → MP4 Video (remux, no re-encode)
    ↓
[Download API] → reel.mp4 (saved to disk)
```

---

## 🎯 Ключевые Технологии (7 основных)

| # | Технология | Роль | Этап |
|---|-----------|------|------|
| 1 | **FFmpeg.wasm** | Обработка видео/аудио в браузере | 1, 2, 4 |
| 2 | **OpenAI Whisper API** | AI транскрипция речи в текст | 2 |
| 3 | **Remotion** | Композиция и рендеринг видео | 3 |
| 4 | **html2canvas** | Захват DOM элементов | 4 |
| 5 | **MediaRecorder API** | Запись canvas stream в видео | 4 |
| 6 | **Canvas API** | Рисование кадров | 4 |
| 7 | **React** | UI компоненты и состояние | Все |

---

## ⏱️ Временные Оценки

| Этап | Время | Что происходит |
|------|-------|----------------|
| Загрузка | 1-5 сек | Чтение файла + метаданные |
| Извлечение аудио | 5-15 сек | FFmpeg конвертация MP4→MP3 |
| Whisper API | 15-40 сек | AI транскрипция на сервере OpenAI |
| Оптимизация | \u003c1 сек | Группировка слов в сегменты |
| Предпросмотр | Мгновенно | Real-time рендеринг |
| Захват кадров | 60-90 сек | html2canvas × 1800 кадров |
| Запись WebM | 60-90 сек | MediaRecorder (параллельно) |
| Remux MP4 | 5-10 сек | FFmpeg копирование потока |
| Скачивание | \u003c1 сек | Сохранение на диск |

**Итого:** ~2-3 минуты для 60-секундного видео

---

## 🎨 Особенности Субтитров

### Karaoke Эффект:
- **Обычные слова:** белый цвет, Montserrat Bold, uppercase
- **Активное слово:** желтый (#DFFF00), Eurostile Italic, масштаб 1.05
- **Анимация:** плавный переход 0.15 секунды
- **Позиция:** 150px от низа экрана

### Автоматическое Масштабирование:
- Если текст длинный (\u003e40 символов) → размер шрифта 40px
- Если текст короткий → размер шрифта 60px

---

## 🚀 Почему Это Круто?

1. **100% в браузере** - нет backend сервера, всё локально
2. **Приватность** - видео не загружается на сервер (кроме аудио в Whisper)
3. **WYSIWYG** - что видишь в preview, то и получаешь в MP4
4. **WebAssembly** - нативная скорость FFmpeg
5. **AI субтитры** - точные временные метки от Whisper
6. **Karaoke** - красивый эффект подсветки слов

---

**Дата:** 2026-02-04  
**Версия:** 1.0  
**Статус:** ✅ Полное понимание технологий
