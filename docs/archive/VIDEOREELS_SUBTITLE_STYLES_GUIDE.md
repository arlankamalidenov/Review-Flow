# 🎨 Полное Руководство по Стилям Субтитров VideoReels

**Дата:** 2026-02-05  
**Версия:** 2.0  
**Файл:** `src/video-reels/SubtitleLayer.tsx`

---

## 📋 Содержание

1. [Обзор Системы Стилей](#обзор-системы-стилей)
2. [Структура Компонента](#структура-компонента)
3. [Все Стили по Элементам](#все-стили-по-элементам)
4. [Karaoke Эффект](#karaoke-эффект)
5. [Автоматическое Масштабирование](#автоматическое-масштабирование)
6. [Анимации](#анимации)
7. [Как Изменить Стили](#как-изменить-стили)

---

## 🎯 Обзор Системы Стилей

### Архитектура:
```
VideoReelsCutter.tsx (передает styleConfig)
    ↓
RemotionPreview.tsx (прокидывает в Player)
    ↓
Composition.tsx (передает в SubtitleLayer)
    ↓
SubtitleLayer.tsx (применяет стили)
```

### Важно:
- ✅ **Все стили inline** (не используется Tailwind)
- ✅ **Чистый CSS** через style prop
- ✅ **Динамические значения** через JavaScript

---

## 🏗️ Структура Компонента

### 3 Уровня Контейнеров:

```tsx
<div>  // Уровень 1: Позиционирование + Анимация
  <div>  // Уровень 2: Ширина + Центрирование
    <div>  // Уровень 3: Padding + Текст
      <span>слово1</span>
      <span>слово2</span>
      <span>слово3</span>
    </div>
  </div>
</div>
```

---

## 📐 Все Стили по Элементам

### 1️⃣ Уровень 1: Главный Контейнер (Позиционирование)

**Файл:** `SubtitleLayer.tsx`, строки 109-123

```tsx
<div style={{
  // Позиционирование
  position: 'absolute',
  bottom: '150px',           // 🔧 Расстояние от низа видео
  left: 0,
  right: 0,
  
  // Layout
  display: 'flex',
  justifyContent: 'center',  // Центрирование по горизонтали
  
  // Отступы
  paddingLeft: '32px',       // 🔧 Отступ слева
  paddingRight: '32px',      // 🔧 Отступ справа
  
  // Анимация (динамические)
  opacity: opacity,          // 0.9 → 1.0 (fade in)
  transform: `scale(${scale})`,  // 0.95 → 1.0 (zoom in)
  
  // Z-index
  zIndex: 9999,              // Поверх всего
}}>
```

#### Что Можно Изменить:
| Параметр | Текущее | Описание | Как Изменить |
|----------|---------|----------|--------------|
| `bottom` | `'150px'` | Расстояние от низа | Увеличить/уменьшить значение |
| `paddingLeft` | `'32px'` | Отступ слева | Изменить значение |
| `paddingRight` | `'32px'` | Отступ справа | Изменить значение |
| `opacity` | `0.9 → 1.0` | Прозрачность | Изменить в строках 82-84 |
| `transform` | `scale(0.95 → 1.0)` | Масштаб | Изменить в строках 78-80 |

---

### 2️⃣ Уровень 2: Контейнер Ширины

**Файл:** `SubtitleLayer.tsx`, строки 125-130

```tsx
<div style={{
  position: 'relative',
  maxWidth: '900px',         // 🔧 Максимальная ширина текста
  width: '100%',
}}>
```

#### Что Можно Изменить:
| Параметр | Текущее | Описание | Как Изменить |
|----------|---------|----------|--------------|
| `maxWidth` | `'900px'` | Макс. ширина блока субтитров | Увеличить для длинных текстов |

---

### 3️⃣ Уровень 3: Текстовый Контейнер

**Файл:** `SubtitleLayer.tsx`, строки 132-143

```tsx
<div style={{
  position: 'relative',
  
  // Padding
  paddingLeft: '24px',       // 🔧 Внутренний отступ слева
  paddingRight: '24px',      // 🔧 Внутренний отступ справа
  paddingTop: '12px',        // 🔧 Внутренний отступ сверху
  paddingBottom: '12px',     // 🔧 Внутренний отступ снизу
  
  // Текст
  textAlign: 'center',       // Центрирование текста
  lineHeight: 1.2,           // 🔧 Межстрочный интервал
  
  // Перенос слов
  wordWrap: 'break-word',
  overflowWrap: 'break-word',
}}>
```

#### Что Можно Изменить:
| Параметр | Текущее | Описание | Как Изменить |
|----------|---------|----------|--------------|
| `paddingLeft/Right` | `'24px'` | Внутренние отступы | Изменить для фона |
| `paddingTop/Bottom` | `'12px'` | Вертикальные отступы | Изменить для фона |
| `lineHeight` | `1.2` | Межстрочный интервал | 1.0-2.0 (больше = больше расстояние) |
| `textAlign` | `'center'` | Выравнивание | `'left'`, `'right'`, `'center'` |

---

### 4️⃣ Стили Слов (Karaoke)

**Файл:** `SubtitleLayer.tsx`, строки 149-172

```tsx
<span style={{
  // Display
  display: 'inline-block',
  marginRight: '0.3em',      // 🔧 Расстояние между словами
  
  // Шрифт
  fontFamily: isActive 
    ? "'Eurostile', sans-serif"     // 🔧 Шрифт активного слова
    : "'Montserrat', sans-serif",   // 🔧 Шрифт обычного слова
  fontSize: `${baseFontSize}px`,    // 🔧 40px или 60px (авто)
  fontWeight: isActive ? 500 : 800, // 🔧 Толщина шрифта
  fontStyle: isActive ? 'italic' : 'normal',  // 🔧 Курсив для активного
  
  // Трансформация текста
  textTransform: isActive ? 'none' : 'uppercase',  // 🔧 ЗАГЛАВНЫЕ для обычных
  
  // Цвет
  color: isActive ? '#DFFF00' : '#FFFFFF',  // 🔧 Желтый / Белый
  
  // Масштаб
  transform: isActive ? 'scale(1.05)' : 'scale(1)',  // 🔧 Увеличение активного
  
  // Анимация
  transition: 'all 0.15s ease-out',  // 🔧 Скорость перехода
  
  // Тень
  textShadow: `
    2px 2px 8px rgba(0, 0, 0, 0.5),    // 🔧 Основная тень
    0 0 12px rgba(0, 0, 0, 0.4)        // 🔧 Свечение
  `,
}}>
```

---

## 🎨 Karaoke Эффект (Детально)

### Обычное Слово:
```tsx
{
  fontFamily: "'Montserrat', sans-serif",
  fontSize: "60px",           // или 40px для длинных текстов
  fontWeight: 800,            // Extra Bold
  fontStyle: 'normal',
  textTransform: 'uppercase', // ВСЕ ЗАГЛАВНЫЕ
  color: '#FFFFFF',           // Белый
  transform: 'scale(1)',      // Обычный размер
}
```

### Активное Слово (Karaoke):
```tsx
{
  fontFamily: "'Eurostile', sans-serif",
  fontSize: "60px",           // или 40px для длинных текстов
  fontWeight: 500,            // Medium
  fontStyle: 'italic',        // Курсив
  textTransform: 'none',      // Как написано
  color: '#DFFF00',           // Ярко-желтый
  transform: 'scale(1.05)',   // Увеличено на 5%
}
```

### Переход:
```tsx
transition: 'all 0.15s ease-out'  // Плавная анимация 150ms
```

---

## 📊 Автоматическое Масштабирование

**Файл:** `SubtitleLayer.tsx`, строки 90-94

### Логика:
```tsx
const textLength = activeSubtitle.text.length;
const estimatedWidth = textLength * 35;  // Примерная ширина в пикселях
const needsScaling = estimatedWidth > 900 || textLength > 40;
const baseFontSize = needsScaling ? 40 : 60;
```

### Правила:
| Условие | Размер Шрифта |
|---------|---------------|
| Текст ≤ 40 символов | **60px** (большой) |
| Текст > 40 символов | **40px** (маленький) |
| Ширина > 900px | **40px** (маленький) |

### Как Изменить:
```tsx
// Строки 90-94
const baseFontSize = needsScaling ? 40 : 60;
//                                  ↑    ↑
//                          маленький  большой

// Изменить пороги:
const needsScaling = estimatedWidth > 900 || textLength > 40;
//                                     ↑                   ↑
//                            макс. ширина        макс. символов
```

---

## 🎬 Анимации

### 1. Появление (Entrance)

**Файл:** `SubtitleLayer.tsx`, строки 76-84

```tsx
// Прогресс анимации (0 → 1)
const entranceProgress = Math.min(subtitleProgress * 3, 1);

// Масштаб (0.95 → 1.0)
const scale = interpolate(entranceProgress, [0, 1], [0.95, 1], {
  extrapolateRight: 'clamp',
});

// Прозрачность (0.9 → 1.0)
const opacity = interpolate(entranceProgress, [0, 1], [0.9, 1], {
  extrapolateRight: 'clamp',
});
```

#### Параметры:
| Параметр | Начало | Конец | Описание |
|----------|--------|-------|----------|
| `scale` | 0.95 | 1.0 | Увеличение с 95% до 100% |
| `opacity` | 0.9 | 1.0 | Появление с 90% до 100% |
| Скорость | `subtitleProgress * 3` | - | В 3 раза быстрее длительности |

#### Как Изменить:
```tsx
// Изменить начальный масштаб
const scale = interpolate(entranceProgress, [0, 1], [0.8, 1], ...);
//                                                     ↑
//                                            начальный масштаб (80%)

// Изменить начальную прозрачность
const opacity = interpolate(entranceProgress, [0, 1], [0.5, 1], ...);
//                                                      ↑
//                                            начальная прозрачность (50%)

// Изменить скорость
const entranceProgress = Math.min(subtitleProgress * 2, 1);
//                                                    ↑
//                                           множитель скорости
```

### 2. Karaoke (Подсветка Слов)

**Файл:** `SubtitleLayer.tsx`, строки 86-88

```tsx
// Разбить текст на слова
const words = activeSubtitle.text.split(' ');

// Вычислить индекс активного слова
const activeWordIndex = Math.floor(subtitleProgress * words.length);
```

#### Логика:
```
Прогресс 0%   → Слово 0 (первое)
Прогресс 25%  → Слово 1 (второе)
Прогресс 50%  → Слово 2 (третье)
Прогресс 75%  → Слово 3 (четвертое)
Прогресс 100% → Слово 4 (последнее)
```

### 3. Переход Между Словами

```tsx
transition: 'all 0.15s ease-out'
```

| Свойство | Значение | Описание |
|----------|----------|----------|
| Длительность | `0.15s` | 150 миллисекунд |
| Функция | `ease-out` | Замедление в конце |
| Свойства | `all` | Все изменяемые свойства |

---

## 🎨 Цветовая Палитра

### Текущие Цвета:

| Элемент | Цвет | HEX | RGB |
|---------|------|-----|-----|
| **Обычное слово** | Белый | `#FFFFFF` | rgb(255, 255, 255) |
| **Активное слово** | Ярко-желтый | `#DFFF00` | rgb(223, 255, 0) |
| **Тень (основная)** | Черный 50% | `rgba(0, 0, 0, 0.5)` | - |
| **Тень (свечение)** | Черный 40% | `rgba(0, 0, 0, 0.4)` | - |

### Где Изменить:
```tsx
// Строки 161
color: isActive ? '#DFFF00' : '#FFFFFF',
//                  ↑           ↑
//            активное      обычное
```

---

## 🔤 Шрифты

### Текущие Шрифты:

| Состояние | Шрифт | Вес | Стиль | Трансформация |
|-----------|-------|-----|-------|---------------|
| **Обычное** | Montserrat | 800 (Extra Bold) | normal | UPPERCASE |
| **Активное** | Eurostile | 500 (Medium) | italic | none |

### Импорты:
```tsx
// Строка 3
import '@fontsource/montserrat/800.css'; // Extra Bold
```

### Где Изменить:
```tsx
// Строки 154-160
fontFamily: isActive 
  ? "'Eurostile', sans-serif"      // 🔧 Активное
  : "'Montserrat', sans-serif",    // 🔧 Обычное

fontWeight: isActive ? 500 : 800,  // 🔧 Вес

fontStyle: isActive ? 'italic' : 'normal',  // 🔧 Курсив

textTransform: isActive ? 'none' : 'uppercase',  // 🔧 Регистр
```

---

## 🎯 Как Изменить Стили

### Метод 1: Через styleConfig (Не Используется)

**Файл:** `VideoReelsCutter.tsx`, строки 586-592

```tsx
styleConfig={{
  fontFamily: 'Inter',        // ⚠️ НЕ используется
  fontSize: 40,               // ⚠️ НЕ используется
  color: '#FFFFFF',           // ⚠️ НЕ используется
  strokeColor: '#BA0C2F',     // ⚠️ НЕ используется
  strokeWidth: 2,             // ⚠️ НЕ используется
}}
```

**Примечание:** Эти параметры передаются, но **не применяются** в SubtitleLayer! Все стили hardcoded.

---

### Метод 2: Прямое Изменение в SubtitleLayer ✅

**Файл:** `src/video-reels/SubtitleLayer.tsx`

#### Пример 1: Изменить Цвета
```tsx
// Строка 161
color: isActive ? '#FF0000' : '#00FF00',
//                  ↑           ↑
//              красный      зеленый
```

#### Пример 2: Изменить Размер Шрифта
```tsx
// Строка 94
const baseFontSize = needsScaling ? 50 : 70;
//                                  ↑    ↑
//                          маленький  большой
```

#### Пример 3: Изменить Позицию
```tsx
// Строка 113
bottom: '200px',  // Выше от низа
```

#### Пример 4: Изменить Тень
```tsx
// Строки 164-167
textShadow: `
  4px 4px 16px rgba(255, 0, 0, 0.8),  // Красная тень
  0 0 20px rgba(255, 0, 0, 0.6)       // Красное свечение
`,
```

#### Пример 5: Изменить Скорость Анимации
```tsx
// Строка 163
transition: 'all 0.3s ease-in-out',  // Медленнее (300ms)
```

#### Пример 6: Убрать Uppercase
```tsx
// Строка 160
textTransform: 'none',  // Не преобразовывать регистр
```

---

## 📝 Полная Карта Параметров

### Позиционирование:
| Параметр | Строка | Значение | Описание |
|----------|--------|----------|----------|
| `bottom` | 113 | `'150px'` | Расстояние от низа |
| `paddingLeft` | 118 | `'32px'` | Отступ слева |
| `paddingRight` | 119 | `'32px'` | Отступ справа |
| `maxWidth` | 128 | `'900px'` | Макс. ширина |

### Текст:
| Параметр | Строка | Значение | Описание |
|----------|--------|----------|----------|
| `fontFamily` (обычное) | 156 | `'Montserrat'` | Шрифт обычного слова |
| `fontFamily` (активное) | 155 | `'Eurostile'` | Шрифт активного слова |
| `fontSize` | 157, 94 | `40px` / `60px` | Размер (авто) |
| `fontWeight` (обычное) | 158 | `800` | Толщина обычного |
| `fontWeight` (активное) | 158 | `500` | Толщина активного |
| `fontStyle` (активное) | 159 | `'italic'` | Курсив |
| `textTransform` (обычное) | 160 | `'uppercase'` | Заглавные |
| `lineHeight` | 140 | `1.2` | Межстрочный интервал |

### Цвета:
| Параметр | Строка | Значение | Описание |
|----------|--------|----------|----------|
| `color` (обычное) | 161 | `'#FFFFFF'` | Белый |
| `color` (активное) | 161 | `'#DFFF00'` | Желтый |

### Эффекты:
| Параметр | Строка | Значение | Описание |
|----------|--------|----------|----------|
| `textShadow` | 164-167 | См. код | Тень + свечение |
| `transform` (активное) | 162 | `'scale(1.05)'` | Увеличение 5% |
| `transition` | 163 | `'all 0.15s ease-out'` | Анимация |

### Анимация Появления:
| Параметр | Строка | Значение | Описание |
|----------|--------|----------|----------|
| `opacity` (начало) | 82 | `0.9` | Начальная прозрачность |
| `opacity` (конец) | 82 | `1.0` | Конечная прозрачность |
| `scale` (начало) | 78 | `0.95` | Начальный масштаб |
| `scale` (конец) | 78 | `1.0` | Конечный масштаб |
| Скорость | 77 | `* 3` | В 3 раза быстрее |

---

## 🔧 Примеры Изменений

### Пример 1: Красные Субтитры
```tsx
// Строка 161
color: isActive ? '#FF0000' : '#FFCCCC',
```

### Пример 2: Больше от Низа
```tsx
// Строка 113
bottom: '250px',
```

### Пример 3: Без Uppercase
```tsx
// Строка 160
textTransform: 'none',
```

### Пример 4: Медленная Анимация
```tsx
// Строка 163
transition: 'all 0.5s ease-in-out',
```

### Пример 5: Больший Шрифт
```tsx
// Строка 94
const baseFontSize = needsScaling ? 60 : 80;
```

---

## 📚 Связанные Файлы

1. **SubtitleLayer.tsx** - Основной компонент стилей
2. **VideoReelsCutter.tsx** - Передает styleConfig (не используется)
3. **Composition.tsx** - Прокидывает props
4. **RemotionPreview.tsx** - Отображает композицию

---

## 🎯 Итоговая Структура Стилей

```
SubtitleLayer
├── Контейнер 1 (Позиционирование)
│   ├── position: absolute
│   ├── bottom: 150px
│   ├── padding: 32px
│   ├── opacity: 0.9 → 1.0
│   └── transform: scale(0.95 → 1.0)
│
├── Контейнер 2 (Ширина)
│   └── maxWidth: 900px
│
├── Контейнер 3 (Текст)
│   ├── padding: 24px / 12px
│   ├── textAlign: center
│   └── lineHeight: 1.2
│
└── Слова (Karaoke)
    ├── Обычное:
    │   ├── Montserrat 800
    │   ├── 40px / 60px
    │   ├── UPPERCASE
    │   ├── #FFFFFF
    │   └── scale(1)
    │
    └── Активное:
        ├── Eurostile 500 italic
        ├── 40px / 60px
        ├── none (как написано)
        ├── #DFFF00
        └── scale(1.05)
```

---

**Последнее обновление:** 2026-02-05  
**Версия:** 2.0  
**Статус:** ✅ Полная документация
