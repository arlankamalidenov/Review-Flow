# ReviewFlow Dashboard - Миграция на start_reviews CPT

## ✅ Выполненные изменения

### 1. **Обновление типов (types.ts)**
Полностью переработаны типы для работы с Custom Post Type `start_reviews`:

```typescript
// Новые статусы для CPT
export enum ReviewStatus {
  Published = 'publish',
  Pending = 'pending',
  Draft = 'draft',
  Trash = 'trash',
}

// ACF поля
export interface StartReviewACF {
  student_name: string;
  bf_email: string;
  review_text: string;
  review_rating: number; // 1-5
  module_number: string;
  review_country_iso: string; // ISO код страны
}

// Основной тип отзыва
export interface StartReview {
  id: number;
  date: string;
  status: ReviewStatus;
  title: RenderedContent;
  content: RenderedContent;
  acf: StartReviewACF;
  // ... другие поля
}
```

### 2. **Обновление wpService**
Изменен базовый endpoint и все функции:

- **Endpoint**: `https://bfisherman.no/wp-json/wp/v2/start_reviews`
- **Функции**:
  - `fetchReviews()` - получение отзывов
  - `updateReviewStatus()` - изменение статуса (publish/pending)
  - `updateReview()` - обновление ACF полей
  - `deleteReview()` - удаление отзыва

### 3. **Маппинг данных из ACF**
Все данные теперь берутся из ACF полей:

| Поле UI | ACF поле | Fallback |
|---------|----------|----------|
| Имя студента | `acf.student_name` | `title.rendered` |
| Email | `acf.bf_email` | - |
| Текст отзыва | `acf.review_text` | - |
| Рейтинг | `acf.review_rating` | - |
| Модуль | `acf.module_number` | - |
| Страна | `acf.review_country_iso` | - |

### 4. **Новый дизайн ReviewCard в стиле Apple**

#### Основные характеристики:
- **Border Radius**: 16px (rounded-2xl)
- **Тени**: Мягкие тени с hover эффектом
- **Цветовая схема**: Минималистичная, белый фон

#### Новые элементы:

**Module Badge** (правый верхний угол):
```tsx
<div className="bg-gradient-to-br from-indigo-500 to-indigo-600 
                text-white text-xs font-bold px-3 py-1.5 
                rounded-full shadow-md">
  Module {review.acf.module_number}
</div>
```

**Флаг страны**:
```tsx
<span className="text-lg">
  {getCountryFlag(review.acf.review_country_iso)} 🇳🇴
</span>
<span className="text-xs text-slate-400">
  {getCountryName(review.acf.review_country_iso)}
</span>
```

**Звездочка для 5-звездочных отзывов**:
```tsx
{isFiveStars && (
  <Star size={16} className="fill-amber-400 text-amber-400" />
)}
```

**Рейтинг звездами**:
```tsx
<StarRating rating={review.acf.review_rating} size={18} />
```

### 5. **Компонент StarRating**
Новый компонент для отображения рейтинга:

```tsx
<StarRating 
  rating={5} 
  maxRating={5} 
  size={18} 
  showNumber={false} 
/>
```

Отображает заполненные звезды (желтые) и пустые (серые).

### 6. **Утилиты для работы со странами**

**getCountryFlag()**:
```typescript
getCountryFlag('NO') // 🇳🇴
getCountryFlag('US') // 🇺🇸
getCountryFlag('RU') // 🇷🇺
```

**getCountryName()**:
```typescript
getCountryName('NO') // "Norway"
getCountryName('US') // "United States"
```

### 7. **Обновленное модальное окно**
EditModal теперь работает с ACF полями:

- **Student Name** → `acf.student_name`
- **Email** → `acf.bf_email`
- **Review Text** → `acf.review_text`

При сохранении данные отправляются в формате:
```typescript
{
  acf: {
    student_name: "...",
    bf_email: "...",
    review_text: "..."
  }
}
```

### 8. **Логика статусов**

**Published** (Опубликовано):
- Статус: `publish`
- Цвет бейджа: Зеленый
- Отображается на сайте

**Pending** (Ожидает):
- Статус: `pending`
- Цвет бейджа: Желтый
- Кнопка "Approve" меняет статус на `publish`

**Draft** (Черновик):
- Статус: `draft`
- Цвет бейджа: Серый

**Trash** (Корзина):
- Статус: `trash`
- Цвет бейджа: Красный

### 9. **Обновленный Sidebar**
Новые фильтры:
- All Reviews
- Pending
- Published
- Draft
- Trash

### 10. **Оптимистичные обновления**
Сохранены для всех операций:
- Approve → Карточка исчезает мгновенно
- Trash → Удаление без задержки
- Edit → Обновление с индикатором загрузки

## 🎨 Визуальные улучшения

### Карточка отзыва:
1. **Аватар**: Градиентный круг с первой буквой имени
2. **Бейдж модуля**: Градиентный фон (indigo)
3. **Флаг страны**: Emoji флаг + название страны
4. **Рейтинг**: 5 звезд (желтые/серые)
5. **Звезда для 5★**: Маленькая золотая звезда рядом с именем
6. **Статус**: Цветной бейдж (зеленый/желтый/серый/красный)

### Анимации:
- Плавное появление карточек (300ms)
- Hover эффект с тенью
- Backdrop blur на модальном окне
- Transition на всех интерактивных элементах

## 📁 Измененные файлы

```
types.ts                    ✅ Полностью переписан
services/wpService.ts       ✅ Полностью переписан
components/
  ├── ReviewCard.tsx        ✅ Полностью переписан (Apple style)
  ├── ReviewList.tsx        ✅ Обновлен для StartReview
  ├── EditModal.tsx         ✅ Обновлен для ACF полей
  ├── DashboardLayout.tsx   ✅ Обновлен для start_reviews
  ├── Sidebar.tsx           ✅ Обновлен для ReviewStatus
  ├── StarRating.tsx        ✨ Новый компонент
  └── ui/Primitives.tsx     ✅ Обновлен StatusBadge
utils/
  └── formatters.ts         ✅ Добавлены функции для стран
```

## 🚀 Результат

### Dashboard показывает:
✅ Карточки в стиле Apple (16px radius, мягкие тени)  
✅ Бейджи модулей в правом верхнем углу  
✅ Флаги и названия стран  
✅ Рейтинг звездами  
✅ Золотая звезда для 5-звездочных отзывов  
✅ Цветные статусы (Published/Pending/Draft/Trash)  
✅ Плавные анимации  

### Модальное окно:
✅ Поля привязаны к ACF (student_name, bf_email, review_text)  
✅ Данные корректно загружаются  
✅ Сохранение через REST API  
✅ Индикатор загрузки  

### Логика:
✅ Approve меняет статус на `publish`  
✅ Trash перемещает в корзину  
✅ Оптимистичные обновления работают  
✅ Фильтрация по статусам  

## 🎯 Технический стек

- **React** + TypeScript
- **Tailwind CSS** (минимализм, Apple-style)
- **Framer Motion** (плавные анимации)
- **Lucide React** (иконки)
- **React Query** (управление состоянием)
- **WordPress REST API** (start_reviews CPT)

## 📝 Примечание о CORS

При работе с `localhost:3000` и `bfisherman.no` может возникать ошибка CORS. 

**Решение**: Настроить на сервере WordPress заголовки:
```php
add_action('rest_api_init', function() {
    header('Access-Control-Allow-Origin: http://localhost:3000');
    header('Access-Control-Allow-Credentials: true');
});
```

Или использовать прокси в `vite.config.ts`.
