# 🎬 ПРОБЛЕМА: Субтитры отсутствуют в скачанном MP4

**Дата:** 2026-01-30 18:00  
**Статус:** 🔍 ДИАГНОСТИКА

---

## 📊 Текущая ситуация:

| Компонент | Статус | Субтитры |
|-----------|--------|----------|
| **Left Panel (RemotionPreview)** | ✅ | ✅ Видны |
| **Right Panel (RemotionPreview)** | ✅ | ✅ Видны |
| **Downloaded MP4 (FFmpeg)** | ❌ | ❌ Отсутствуют |

---

## 🔍 Причина:

**Скачанный MP4 создается через FFmpeg, а не через Remotion!**

```tsx
// handleCreateReel вызывает:
const output = await processCompleteReel(
    videoFile.file,
    reelConfig.startTime,
    reelConfig.duration,
    adjustedSubtitles,  // ← Передаются в FFmpeg
    reelConfig.subtitleStyle,
    ...
);
```

FFmpeg **пытается** добавить субтитры через SRT файл и `subtitles` filter, но что-то идет не так.

---

## 🛠️ Что делает FFmpeg:

### Шаг 1: Генерация SRT файла
```typescript
function generateSRT(subtitles) {
    return subtitles
        .map((sub, index) => {
            const startTime = formatSRTTime(sub.start);
            const endTime = formatSRTTime(sub.end);
            return `${index + 1}\n${startTime} --> ${endTime}\n${sub.text}\n`;
        })
        .join('\n');
}
```

### Шаг 2: Применение субтитров
```bash
ffmpeg -i input.mp4 \
  -vf "subtitles=subtitles.srt:fontsdir=.:force_style='...'" \
  -c:v libx264 \
  output.mp4
```

---

## 🔧 Возможные проблемы:

### 1. **Шрифт не загружается** ❓
```typescript
const fontResponse = await fetch(`/fonts/${fontName}`);
// Если шрифт не найден, FFmpeg не сможет отрендерить текст
```

**Проверка:**
- Есть ли файл `/public/fonts/Inter-Bold.ttf`?
- Доступен ли он по HTTP?

### 2. **SRT формат неправильный** ❓
```
1
00:00:01,000 --> 00:00:03,000
Hello World

2
00:00:03,000 --> 00:00:05,000
Second subtitle
```

**Проверка:**
- Добавлен `console.log` для просмотра SRT содержимого
- Проверьте консоль браузера при экспорте

### 3. **FFmpeg команда не работает** ❓
```bash
-vf "subtitles=subtitles.srt:fontsdir=.:force_style='...'"
```

**Проверка:**
- FFmpeg.wasm может не поддерживать `subtitles` filter
- Нужно проверить логи FFmpeg

---

## 📋 ИНСТРУКЦИИ ДЛЯ ТЕСТИРОВАНИЯ:

### Шаг 1: Откройте консоль браузера
1. Нажмите `F12` или `Cmd+Option+I`
2. Перейдите на вкладку "Console"

### Шаг 2: Создайте рил
1. Загрузите видео
2. Нажмите "Create Reel"
3. Дождитесь завершения

### Шаг 3: Проверьте логи
Найдите в консоли:
```
📝 [Subtitles] SRT Content Preview: ...
```

**Отправьте мне:**
1. Содержимое SRT preview
2. Все сообщения с `[Subtitles]`
3. Любые ошибки FFmpeg

---

## 🎯 Возможные решения:

### Решение 1: **Проверить шрифт** ✅
```bash
# Убедитесь что файл существует:
ls public/fonts/Inter-Bold.ttf
```

Если нет, скачайте Inter font:
https://fonts.google.com/specimen/Inter

### Решение 2: **Использовать встроенный шрифт** ✅
Изменить код чтобы использовать системный шрифт вместо загрузки:
```typescript
// Вместо загрузки шрифта, использовать Arial/Helvetica
const forceStyle = [
    `FontName=Arial`,  // ← Системный шрифт
    ...
].join(',');
```

### Решение 3: **Использовать drawtext вместо subtitles** ✅
```bash
# Вместо subtitles filter, использовать drawtext:
-vf "drawtext=text='Hello':x=10:y=10:fontsize=40:fontcolor=white"
```

### Решение 4: **Использовать Remotion Cloud** ☁️
Самое надежное решение - использовать Remotion Lambda:
- Профессиональный rendering
- Гарантированно работает
- Требует AWS аккаунт

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ:

1. **Проверьте консоль** и отправьте логи
2. **Проверьте наличие шрифта** в `/public/fonts/`
3. **Попробуйте скачать видео** и проверьте есть ли субтитры

**После получения логов, я смогу точно определить проблему!**

---

**Подготовлено:** 2026-01-30 18:00  
**Статус:** 🔍 ОЖИДАНИЕ ДИАГНОСТИКИ
