# 📝 Шпаргалка: Стили Субтитров VideoReels

**Быстрый справочник для изменения стилей**

---

## 🎯 Главное

**Файл для изменений:** `src/video-reels/SubtitleLayer.tsx`

**Важно:** Все стили **hardcoded** (жестко заданы) в коде. `styleConfig` из `VideoReelsCutter.tsx` **НЕ используется**.

---

## 🎨 Быстрые Изменения

### 1. Изменить Цвета

**Строка 161:**
```tsx
color: isActive ? '#DFFF00' : '#FFFFFF',
//                  ↑           ↑
//              желтый      белый
```

**Примеры:**
- Красный активный: `'#FF0000'`
- Синий обычный: `'#0000FF'`
- Зеленый: `'#00FF00'`

---

### 2. Изменить Позицию

**Строка 113:**
```tsx
bottom: '150px',  // Расстояние от низа
```

**Примеры:**
- Выше: `'200px'`, `'250px'`
- Ниже: `'100px'`, `'50px'`

---

### 3. Изменить Размер Шрифта

**Строка 94:**
```tsx
const baseFontSize = needsScaling ? 40 : 60;
//                                  ↑    ↑
//                          маленький  большой
```

**Примеры:**
- Больше: `50 : 70`
- Меньше: `30 : 50`

---

### 4. Изменить Шрифты

**Строки 154-156:**
```tsx
fontFamily: isActive 
  ? "'Eurostile', sans-serif"      // Активное
  : "'Montserrat', sans-serif",    // Обычное
```

**Примеры:**
- Arial: `'Arial, sans-serif'`
- Times: `'Times New Roman, serif'`
- Roboto: `'Roboto, sans-serif'`

---

### 5. Убрать ЗАГЛАВНЫЕ Буквы

**Строка 160:**
```tsx
textTransform: isActive ? 'none' : 'uppercase',
//                                    ↑
//                          Изменить на 'none'
```

---

### 6. Изменить Скорость Анимации

**Строка 163:**
```tsx
transition: 'all 0.15s ease-out',
//                ↑
//          150 миллисекунд
```

**Примеры:**
- Быстрее: `'0.1s'` (100ms)
- Медленнее: `'0.3s'` (300ms)

---

### 7. Изменить Тень

**Строки 164-167:**
```tsx
textShadow: `
  2px 2px 8px rgba(0, 0, 0, 0.5),
  0 0 12px rgba(0, 0, 0, 0.4)
`,
```

**Формат:** `X Y Blur Color`
- `2px` - сдвиг вправо
- `2px` - сдвиг вниз
- `8px` - размытие
- `rgba(0, 0, 0, 0.5)` - цвет (черный 50%)

---

### 8. Изменить Отступы

**Строки 118-119:**
```tsx
paddingLeft: '32px',   // Слева
paddingRight: '32px',  // Справа
```

---

## 📊 Все Параметры (Таблица)

| Что Изменить | Строка | Параметр | Текущее | Примеры |
|--------------|--------|----------|---------|---------|
| **Позиция от низа** | 113 | `bottom` | `'150px'` | `'200px'`, `'100px'` |
| **Цвет обычный** | 161 | `color` | `'#FFFFFF'` | `'#FF0000'`, `'#00FF00'` |
| **Цвет активный** | 161 | `color` | `'#DFFF00'` | `'#FF0000'`, `'#00FFFF'` |
| **Размер большой** | 94 | `baseFontSize` | `60` | `70`, `80` |
| **Размер маленький** | 94 | `baseFontSize` | `40` | `50`, `30` |
| **Шрифт обычный** | 156 | `fontFamily` | `'Montserrat'` | `'Arial'`, `'Roboto'` |
| **Шрифт активный** | 155 | `fontFamily` | `'Eurostile'` | `'Arial'`, `'Roboto'` |
| **Заглавные** | 160 | `textTransform` | `'uppercase'` | `'none'`, `'lowercase'` |
| **Скорость** | 163 | `transition` | `'0.15s'` | `'0.3s'`, `'0.1s'` |
| **Отступы** | 118-119 | `padding` | `'32px'` | `'50px'`, `'20px'` |

---

## 🎬 Karaoke Эффект

### Обычное Слово:
- Шрифт: **Montserrat 800** (жирный)
- Цвет: **#FFFFFF** (белый)
- Регистр: **UPPERCASE** (заглавные)
- Масштаб: **1.0** (обычный)

### Активное Слово:
- Шрифт: **Eurostile 500 italic** (курсив)
- Цвет: **#DFFF00** (желтый)
- Регистр: **none** (как написано)
- Масштаб: **1.05** (увеличено на 5%)

---

## 🔧 Пошаговая Инструкция

### Как Изменить Цвет:

1. Открыть `src/video-reels/SubtitleLayer.tsx`
2. Найти строку 161
3. Изменить `'#DFFF00'` на нужный цвет
4. Сохранить файл
5. Проверить в браузере (HMR обновит автоматически)

### Как Изменить Позицию:

1. Открыть `src/video-reels/SubtitleLayer.tsx`
2. Найти строку 113
3. Изменить `'150px'` на нужное значение
4. Сохранить файл
5. Проверить в браузере

---

## 📐 Структура (Упрощенно)

```
<div>  ← Позиция (bottom: 150px)
  <div>  ← Ширина (maxWidth: 900px)
    <div>  ← Padding (24px)
      <span>СЛОВО1</span>  ← Белый, Montserrat
      <span>слово2</span>  ← Желтый, Eurostile (активное)
      <span>СЛОВО3</span>  ← Белый, Montserrat
    </div>
  </div>
</div>
```

---

## 🎨 Примеры Готовых Стилей

### Стиль 1: Красный Акцент
```tsx
// Строка 161
color: isActive ? '#FF0000' : '#FFFFFF',
```

### Стиль 2: Синий Минимализм
```tsx
// Строка 161
color: isActive ? '#00BFFF' : '#E0E0E0',

// Строка 160
textTransform: 'none',  // Без заглавных
```

### Стиль 3: Зеленый Неон
```tsx
// Строка 161
color: isActive ? '#00FF00' : '#FFFFFF',

// Строки 164-167
textShadow: `
  0 0 20px rgba(0, 255, 0, 0.8),
  0 0 40px rgba(0, 255, 0, 0.6)
`,
```

### Стиль 4: Крупный Текст
```tsx
// Строка 94
const baseFontSize = needsScaling ? 60 : 80;

// Строка 113
bottom: '200px',
```

---

## ⚠️ Важные Замечания

1. **styleConfig НЕ работает** - все стили в SubtitleLayer.tsx
2. **Inline стили** - не используется Tailwind CSS
3. **HMR работает** - изменения видны сразу после сохранения
4. **Шрифты** - Montserrat импортирован, Eurostile должен быть в системе

---

## 📚 Полная Документация

Для подробного описания всех параметров смотри:
- **VIDEOREELS_SUBTITLE_STYLES_GUIDE.md** - полное руководство

---

**Дата:** 2026-02-05  
**Версия:** 1.0  
**Файл для изменений:** `src/video-reels/SubtitleLayer.tsx`
