# 🔍 ДИАГНОСТИКА СУБТИТРОВ - ШАГ ЗА ШАГОМ

## ✅ Что добавлено:

### Подробное логирование в SubtitleLayer.tsx:

```tsx
// Frame 0: Логирование props
console.log('🎬 [SubtitleLayer] Props:', {
    subtitlesCount,
    subtitles,
    styleConfig,
});

// Каждые 30 кадров: Поиск активного субтитра
console.log('🔍 [SubtitleLayer] Search:', {
    currentTime,
    activeSubtitle,
});

// Каждые 30 кадров: Рендеринг
console.log('✅ [SubtitleLayer] Rendering:', {
    text,
    words,
    activeWordIndex,
    baseFontSize,
    opacity,
    scale,
});

// Каждые 60 кадров: Нет активного субтитра
console.log('⚠️ [SubtitleLayer] No active subtitle at time:', currentTime);
```

---

## 🧪 КАК ПРОВЕРИТЬ:

### Шаг 1: Жесткое обновление
```bash
Cmd+Shift+R (или Ctrl+Shift+R)
```

### Шаг 2: Откройте консоль
```bash
Cmd+Option+I (или F12)
Вкладка: Console
```

### Шаг 3: Создайте рил
```
1. Загрузите видео
2. Нажмите "Create Reel"
3. Дождитесь генерации субтитров
```

### Шаг 4: Проверьте логи в консоли

**Что искать:**

#### A. Props Logging (Frame 0):
```
🎬 [SubtitleLayer] Props: {
    subtitlesCount: 8,
    subtitles: [
        { start: 0, end: 2.5, text: "первый субтитр..." },
        { start: 2.5, end: 5, text: "второй субтитр..." },
        ...
    ],
    styleConfig: { ... }
}
```

**Проверьте:**
- ✅ `subtitlesCount > 0` (есть субтитры)
- ✅ `subtitles` массив не пустой
- ✅ Каждый subtitle имеет `start`, `end`, `text`

**Если subtitlesCount = 0:**
- ❌ Субтитры не сгенерировались
- Проверьте "Create Reel" процесс
- Проверьте OpenAI API key

#### B. Search Logging (Каждые 30 кадров):
```
🔍 [SubtitleLayer] Search: {
    frame: 30,
    currentTime: "1.00",
    activeSubtitle: {
        text: "показываю, как решить эту проблему",
        start: 0,
        end: 3
    }
}
```

**Проверьте:**
- ✅ `activeSubtitle` не `"NONE"`
- ✅ `currentTime` находится между `start` и `end`

**Если activeSubtitle = "NONE":**
- ❌ Субтитр не найден для текущего времени
- Проверьте timing субтитров
- Возможно gap между субтитрами

#### C. Rendering Logging (Каждые 30 кадров):
```
✅ [SubtitleLayer] Rendering: {
    text: "показываю, как решить эту проблему",
    words: 5,
    activeWordIndex: 2,
    baseFontSize: 60,
    opacity: "1.00",
    scale: "1.00"
}
```

**Проверьте:**
- ✅ `text` не пустой
- ✅ `words > 0`
- ✅ `baseFontSize = 60` или `40`
- ✅ `opacity > 0` (иначе невидим!)
- ✅ `scale > 0` (иначе невидим!)

**Если opacity = 0:**
- ❌ Субтитр невидим из-за анимации
- Проверьте `entranceProgress`

#### D. No Subtitle Logging (Каждые 60 кадров):
```
⚠️ [SubtitleLayer] No active subtitle at time: 5.50
```

**Это нормально если:**
- Между субтитрами есть gaps
- Видео закончилось

**Это проблема если:**
- Постоянно показывается
- Нет других логов

---

## 🔍 ВОЗМОЖНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ:

### Проблема 1: Нет логов вообще
**Причина:** Компонент не рендерится

**Проверьте:**
```bash
# Есть ли ошибки в консоли?
# Красный текст = ошибка
```

**Решение:**
- Проверьте ошибки компиляции
- Проверьте импорты
- Жесткое обновление

### Проблема 2: subtitlesCount = 0
**Причина:** Субтитры не сгенерировались

**Проверьте:**
```tsx
// В VideoReelsCutter.tsx
console.log('Subtitles:', subtitles);
```

**Решение:**
- Проверьте "Create Reel" процесс
- Проверьте OpenAI API key
- Проверьте transcription

### Проблема 3: activeSubtitle = "NONE"
**Причина:** Timing субтитров некорректен

**Проверьте:**
```tsx
// Timing субтитров
subtitles.forEach(sub => {
    console.log(`${sub.start} - ${sub.end}: ${sub.text}`);
});
```

**Решение:**
- Проверьте что `start < end`
- Проверьте что нет gaps
- Проверьте что timing соответствует видео

### Проблема 4: opacity = 0 или scale = 0
**Причина:** Анимация скрывает субтитр

**Проверьте:**
```tsx
// entranceProgress
const entranceProgress = Math.min(subtitleProgress * 3, 1);
```

**Решение:**
- Проверьте `subtitleProgress`
- Проверьте `subtitleDuration`
- Временно уберите анимацию:
```tsx
const opacity = 1; // Force visible
const scale = 1; // Force normal size
```

### Проблема 5: Шрифт не загружается
**Причина:** Eurostile или Montserrat не загрузились

**Проверьте:**
```bash
# DevTools → Network → Fonts
# Должны увидеть:
# - Montserrat (от Google Fonts)
# - Eurostile (от /fonts/)
```

**Решение:**
- Проверьте styles.css (@font-face)
- Проверьте public/fonts/fonnts.com-Eurostile_Medium_Italic.otf
- Откройте http://localhost:5173/font-test.html

---

## 📋 ЧЕКЛИСТ ДИАГНОСТИКИ:

```
□ Жесткое обновление (Cmd+Shift+R)
□ Консоль открыта
□ Создан рил с субтитрами
□ Проверены логи:
  □ 🎬 Props (subtitlesCount > 0)
  □ 🔍 Search (activeSubtitle найден)
  □ ✅ Rendering (opacity > 0, scale > 0)
□ Проверены шрифты:
  □ Network → Fonts
  □ Montserrat загружен
  □ Eurostile загружен
□ Проверены ошибки:
  □ Нет красного текста в консоли
  □ Нет warnings
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ:

1. **Обновите страницу** (Cmd+Shift+R)
2. **Откройте консоль** (Cmd+Option+I)
3. **Создайте рил**
4. **Скопируйте ВСЕ логи** из консоли
5. **Отправьте мне логи** для анализа

**Я найду проблему!** 🔍

---

**Обновлено:** 2026-01-31 14:00
